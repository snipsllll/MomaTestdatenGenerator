/*import {IBuchung, IGeplanteAusgabenKategorie} from './Interfaces';

export class Generator {

  constructor() {

  }

  getRandomBuchungskategorie(id: number):{ id: number; name: string } {
    return {
      id: id,
      name: `Buchungs-Kategorie ${id}`
    }
  }

  getRandomBuchungskategorien(count: number): { id: number; name: string }[] {
    const list: { id: number; name: string }[] = [];

    for (let i = 0; i < count; i++) {
      list.push(this.getRandomBuchungskategorie(i))
    }

    return list;
  }

  getRandomBuchung(): IBuchung {

  }

  getRandomBuchungen(count: number): IBuchung[] {
    const list: IBuchung[] = [];

    for (let i = 0; i < count; i++) {
      list.push(this.getRandomBuchung())
    }

    return list;
  }

  getRandomGeplanteAusgabenKategorie(): IGeplanteAusgabenKategorie {

  }

  getRandomGeplanteAusgabenKategoriem(count: number): IGeplanteAusgabenKategorie[] {
    const list: IGeplanteAusgabenKategorie[] = [];

    for (let i = 0; i < count; i++) {
      list.push(this.getRandomGeplanteAusgabenKategorie())
    }

    return list;
  }
}*/
